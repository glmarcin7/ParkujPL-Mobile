<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.parkujznami.parkujpl_mobile" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".ParkujPL_Mobile"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/manifest.label.app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.StartActivity"
            android:label="@string/manifest.label.start_activity"
            android:noHistory="true"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ParkingListActivity"
            android:label="@string/manifest.label.parking_list_activity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.ChooseNumberOfFreeSpotsActivity"
            android:label="@string/manifest.label.choose_number_of_parking_activity"
            android:screenOrientation="portrait" />

    </application>

</manifest>
